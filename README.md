
  # Create based on prompt

  This is a code bundle for Create based on prompt. The original project is available at https://www.figma.com/design/tzvCw6NC74v4RICto5XGme/Create-based-on-prompt.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  